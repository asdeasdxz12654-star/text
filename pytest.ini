testpaths = tests
[pytest]
# Generate both JUnit XML and HTML reports, stop early on failure
addopts = -q --junitxml=reports/junit.xml --html=reports/report.html --self-contained-html --maxfail=1
testpaths = tests
markers =
    e2e: end-to-end tests
    login: login-related tests
    smoke: quick smoke tests
